# start `heartbeat.conf` server block at `/etc/nginx/sites-enabled/heartbeat.conf`
server {#hostname, http (no 301 redirect to https), PHP fastcgi
    access_log /var/log/nginx/trident.textpattern.com.access.log ipscrubbed;
    #nested variable for Content Security Policy maintainability
    set $csp_http_heartbeat '';
    set $csp_http_heartbeat '${csp_http_heartbeat}base-uri \'self\';';
    set $csp_http_heartbeat '${csp_http_heartbeat}connect-src \'self\';';
    set $csp_http_heartbeat '${csp_http_heartbeat}default-src \'none\';';
    set $csp_http_heartbeat '${csp_http_heartbeat}font-src \'self\';';
    set $csp_http_heartbeat '${csp_http_heartbeat}form-action \'self\';';
    set $csp_http_heartbeat '${csp_http_heartbeat}frame-ancestors \'none\';';
    set $csp_http_heartbeat '${csp_http_heartbeat}img-src \'self\';';
    set $csp_http_heartbeat '${csp_http_heartbeat}manifest-src \'self\';';
    set $csp_http_heartbeat '${csp_http_heartbeat}media-src \'self\';';
    set $csp_http_heartbeat '${csp_http_heartbeat}script-src \'self\';';
    set $csp_http_heartbeat '${csp_http_heartbeat}style-src \'self\';';
    add_header Content-Security-Policy $csp_http_heartbeat;
    add_header Referrer-Policy strict-origin;
    add_header X-Content-Type-Options nosniff;
    add_header X-Frame-Options DENY;
    add_header X-XSS-Protection "1; mode=block";
    error_log /var/log/nginx/trident.textpattern.com.error.log error;
    index index.html index.php;
    listen [::]:80;
    listen 80;
    #FORCETLS#return 301 https://$host$request_uri;
    root /var/www/vhosts/_heartbeat/live/;
    server_name trident.textpattern.com;
    location ^~ /.well-known {
        allow all;
        default_type "text/plain";
        root /var/www/vhosts/_heartbeat/_well-known/;
        try_files $uri/ $uri =404;
    }
    location /favicon.ico {
        access_log off;
        log_not_found off;
    }
    location /robots.txt {
        access_log off;
        log_not_found off;
    }
    location ~ /\. {
        deny all;
    }
    location / {
        index index.html index.php;
        limit_except GET HEAD POST {
            deny all;
        }
        try_files $uri $uri/ =404;
    }
}

server {#hostname, https, PHP fastcgi, check https://cipherli.st for current `add_header` stuff
    access_log /var/log/nginx/trident.textpattern.com.access.log ipscrubbed;
    #nested variable for Content Security Policy maintainability
    set $csp_heartbeat '';
    set $csp_heartbeat '${csp_heartbeat}base-uri \'self\';';
    set $csp_heartbeat '${csp_heartbeat}connect-src \'self\';';
    set $csp_heartbeat '${csp_heartbeat}default-src \'none\';';
    set $csp_heartbeat '${csp_heartbeat}font-src \'self\';';
    set $csp_heartbeat '${csp_heartbeat}form-action \'self\';';
    set $csp_heartbeat '${csp_heartbeat}frame-ancestors \'none\';';
    set $csp_heartbeat '${csp_heartbeat}img-src \'self\';';
    set $csp_heartbeat '${csp_heartbeat}manifest-src \'self\';';
    set $csp_heartbeat '${csp_heartbeat}media-src \'self\';';
    set $csp_heartbeat '${csp_heartbeat}script-src \'self\';';
    set $csp_heartbeat '${csp_heartbeat}style-src \'self\';';
    add_header Content-Security-Policy $csp_heartbeat;
    add_header Referrer-Policy strict-origin;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload";
    add_header X-Content-Type-Options nosniff;
    add_header X-Frame-Options DENY;
    add_header X-XSS-Protection "1; mode=block";
    error_log /var/log/nginx/trident.textpattern.com.error.log error;
    index index.html index.php;
    listen [::]:443 http2 ssl;
    listen 443 http2 ssl;
    root /var/www/vhosts/_heartbeat/live/;
    server_name trident.textpattern.com;
    ssl_certificate /etc/letsencrypt/live/trident.textpattern.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/trident.textpattern.com/privkey.pem;
    ssl_trusted_certificate /etc/letsencrypt/live/trident.textpattern.com/fullchain.pem;
    location ^~ /.well-known {
        allow all;
        default_type "text/plain";
        root /var/www/vhosts/_heartbeat/_well-known/;
        try_files $uri/ $uri =404;
    }
    location /favicon.ico {
        access_log off;
        log_not_found off;
    }
    location /robots.txt {
        access_log off;
        log_not_found off;
    }
    location ~ /\. {
        deny all;
    }
    location / {
        index index.html index.php;
        limit_except GET HEAD POST {
            deny all;
        }
        try_files $uri $uri/ =404;
    }
}
#end `heartbeat` server block
